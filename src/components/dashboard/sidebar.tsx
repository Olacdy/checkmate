import { FC } from 'react';

type sidebarProps = {};

const sidebar: FC<sidebarProps> = ({}) => {
  return (
    <section className='min-h-screen w-full max-w-lg'>
      <div></div>
    </section>
  );
};

export default sidebar;
